#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n;
    cin >> n;
    for (int i = 0; i <= sqrt(n); i++)
    {
        for (int i = 0; i <= sqrt(n); i++)
        {
            cout << "hello" << endl;
        }
    }
    return 0;
}